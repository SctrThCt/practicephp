<?php 

if (count($categories) == 0) {
    echo "элементов нет";
  } else {
    echo count($categories);
  }

  if (count($publisher) == 0) {
    echo "элементов нет";
  } else {
    echo count($publisher);
  }
?>